pipeline {
    agent any
    options{
        ansiColor('xterm')
    }
    stages {
        stage('Build') {
            steps {
                echo 'Running build automation'
            }
        }
    }
}
